import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // eslint and typescript ignore options are not supported in next.config.ts for this version
  // We must fix the errors instead.
};

export default nextConfig;
